# MLBMarket

Run the local website with:

```powershell
python server.py
```

Then open http://127.0.0.1:8000.

Accounts, balances, sessions, and lobby seats are stored in `your_lineup.db`, which is created automatically the first time the server starts.
